
export class Door {
    id: number;
    accessLevel: number;

    constructor(id: number, accessLevel: number) {
        this.id = id;
        this.accessLevel = accessLevel;
    }

    // Dodatkowe metody...
}